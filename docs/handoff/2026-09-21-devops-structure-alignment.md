# Звіт: адаптація структури до гілок команди

- Автор: DevOps, підготовлено за допомогою Codex.
- Дата: 2026-09-21.
- Статус: частково завершено; локальні доповнення, ще без PR і злиття.
- Перевірені версії: origin/main aad9f98, origin/feat/voting-core-models-and-mocks e8da600.
- Кому: обидві frontend-ролі, Backend Core та Backend Auth.

## Що зроблено

Прибрано 81 раніше створений порожній файл альтернативної структури.
Збережено backend-організацію api/v1, models, schemas, services; додано
лише порожні місця для спільного запуску, конфігурації, БД та Auth.
Frontend доповнено папками для сторінок і спільних модулів без дублювання
наявних App.tsx, main.tsx, CSS, конфігурацій та компонента кнопки.
README обох компонентів описують актуальні файли й відповідальність.
Код із незлитої backend-гілки не копіювався.

## Перевірки

Перевірено локальні посилання документації й git diff --check.
Нові файли реалізації порожні. Запуск застосунків не перевірявся.

## Обмеження та наступні кроки

- git fetch виконано. Оновлення локальної main через merge --ff-only було
  відхилене користувачем; локальна база залишається d3b7b67.
- Перед PR треба перенести ці доповнення на актуальну main. У frontend/README.md
  зберегти новий опис; врахувати, що README також змінено frontend-авторкою.
- Backend Core зливає свою реалізацію окремим PR; Auth підключає реальну
  сесію й БД замість заглушок. Нові порожні Auth-файли звірити з її роботою,
  якщо з'явиться відповідна віддалена гілка.
- Не створювати другу модель User або другий SQLAlchemy Base.
- Frontend-авторці перевірити фізичний шлях @/components/ui/button.tsx
  проти alias @ → src та імпорт @/lib/utils.
- Синхронізувати API-контракт із реалізацією: /vote і candidate_id.
